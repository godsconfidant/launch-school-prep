 /* In code show below, identify the following items:

 THE FUNCTION ARGUMENTS:      
  the arguments of a function are passed when the function is called
  ex;

  function hello(name) {
    return 'Hello ' + name;
  }

  hello('Bob') <<<<<<------  Bob is the Function Argument



THE FUNCTION BODY:
  The function body is the code placed between the {}
      ex:

      function hello() {
        --THIS IS THE FUNCTION BODY
      }


THE FUNCTION DECLARATION:
      The function declaration is a function where the word Function comes before any other text;
        ex:

        function funcDec(zeroOrMoreAurguments) {
          return 'function declaration';
        }





THE FUNCTION INVOCATION:
        The noun of a call function.    calling would be the verb invoking

       ex:

       
THE FUNCTION NAME
        This is the name given to the function; What is used to call the function aswell
        ex: 

        function functionName() {
          body
        }

        functionName();


THE FUNCTION PARAMETERS:
        This is the part of the function that takes out of scope information and passes it into the function;
        ex:
                              parameters    
        function greeting(firstName, lastName) {
            return 'Hello ' + firsName + ' ' + lastNmae;
        }

        greeting('Bob', Smith) ---these are aurguments.


THE FUNCTION RETURN VALUE:
        This is the part of the function that sends information out of the function. When the function return value is ran, it ends the function.
        ex:

        function sayHello(name) {
          
          return 'Hello, ' + name;  -------->>> This return line sends that code back out of the function
        }

        sayHello('Bob;);


THE NAMES OF ALL VARIABLES IN THIS PROGRAM:

        function multiplyNumbers(num1, num2, num3) {
          let result = num1 * num2 * num3;
          return result;
        }

        let product = multiplyNumbers(2, 3, 4);

    ---->>> THE VARIABLES:

          multiplyNumbers
          num1, num2, num3
          result;
          product;

*/