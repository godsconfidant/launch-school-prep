let tweet = 'Starting to get the hang of it... #javascript #launchschool';


console.log(tweet.split(' ')); // This will return string as an array, each word an element;
console.log(tweet.split(' ').reverse());// => Tweet as an array reversed;
console.log(tweet.split(' ').reverse().join()); // => Tweet will be a reversed string;